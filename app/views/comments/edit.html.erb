<h1>Editer un commantaire :</h1>
<%= form_with(model: @comment, url: gossip_comment_path(@gossip,@comment), method: :patch) do |form| %>
  <div class = "form-group">
    <%= form.label :content, "Comment" %>
    <%= form.text_field :content, class:"form-control" %>
  </div>  
  <div class = "form-group">    
    <button type="submit" class="btn btn-primary">Modifier le commentaire</button>
  </div>
<% end %>

<%= link_to "Destroy", gossip_comment_path(@gossip,@comment), data: {turbo: true,turbo_method: :delete,turbo_confirm: "Are you sure?"}, class:"btn btn-danger" %> <!--il faut mettre gossip_path(@gossip) pour indiquer quelle élément on veut supprimer -->
<!--Pour pouvoir utiliser turbo_confirm il faut réactiver turbo:true tout en insérarnt la bonne ligne dans application et gemfile-->